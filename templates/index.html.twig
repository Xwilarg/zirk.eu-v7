{% extends 'base.html.twig' %}

{% block content %}
    <div id="intro" class="container">
        <h2>Zirk</h2>
        <p>
            Welcome on my amazing website, I'm a software developer currently working at <a href="https://bionomeex.com" target="_blank">BionomeeX</a><br/>
            As a hobbies, I like to either work on <a class="main-link" href="#project" data-target="project">my own projects</a> (softwares, bots, websites...) or to participate at <a class="main-link" href="#gamejam" data-target="gamejam">gamejams</a> (making a games in usually less than 72 hours)<br/>
            Outside of programming I also have <a class="main-link" href="#about" data-target="about">various others hobbies</a> such as playing video games or reading books<br/>
        </p>
    </div>
    <div class="container">
        <div class="flex">
            <a class="see-more main-link not-link" href="#project" data-category="global-tab" data-target="project">
                Projects
            </a>
            <a class="see-more main-link not-link" href="#gamejam" data-category="global-tab" data-target="gamejam">
                Game Jams
            </a>
            <div class="flex-break"></div>
            <a class="see-more main-link not-link secondary" href="#about" data-category="global-tab" data-target="about">
                About Me
            </a>
            <div class="flex-break"></div>
            <a class="see-more main-link not-link secondary" href="#faq" data-category="global-tab" data-target="faq">
                FAQ
            </a>
        </div>
    </div>
    <div class="global-tab" id="gamejam" hidden>
        <h2>Game Jams</h2>
        <p class="explanation">
            Gamejams are event when you need to make a game under a certain time (usually 48 or 72 hours for the most common events) given a certain theme<br/>
            Jams here are all the ones I did (where we managed to submit something before the deadline)<br/>
            You probably want to open this page on desktop since these games usually aren't designed for mobile
        </p>
        <h3>Best Scores (≥24h)</h3>
        <div class="flex">
            {% for jam in jams | filter(x => x.duration >= 24) | sort((a, b) => a.score <=> b.score)[:3] %}
                {% include 'gamejam.html.twig' %}
            {% endfor %}
        </div>
        <hr/>
        <div class="flex">
            {% for jam in jams %}
                {% include 'gamejam.html.twig' %}
            {% endfor %}
        </div>
    </div>
    <div class="global-tab" id="project" hidden>
        <h2>Projects</h2>
        <p class="explanation">
            There are the list of the projects I'm currently working on<br/>
            If you scroll down a bit more you can get a more or less complete list of all the projects I worked on
        </p>
        <div class="flex">
            {% for project in projects | filter(x => x.highlight) %}
                {% include 'project.html.twig' %}
            {% endfor %}
        </div>
        <div>
            <table>
                <thead>
                    <th>Name</th>
                    <th>Type</th>
                    <th>Description</th>
                    <th>Date</th>
                    <th>Languages</th>
                    <th>Prev.</th>
                </thead>
                <tbody>
                    {% for project in projects %}
                    <tr>
                        <td  class="project-name">{{ project.name }}</td>
                        <td>{{ project.type }}</td>
                        <td>{{ project.description }}</td>
                        <td>{{ project.start }} - {{ project.end }}</td>
                        <td>{{ project.languages | join(" ") }}</td>
                        <td class="project-image-preview" data-name="{{ project.name }}" data-img="{{ project.image }}">{% if project.image is not null %}<i class="fa-solid fa-computer-mouse"></i>{% endif %}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            <div id="project-normal-preview" hidden>
                <h2 id="project-normal-preview-title"></h2>
                <img id="project-normal-preview-image" src=""/>
            </div>
        </div>
    </div>
    <div class="global-tab" id="about" hidden>
        <h2>About</h2>
        <h3>Social</h3>
        <p>
            {% for social in about.social %}
            <a class="logo not-link" href="{{ social.link }}" target="_blank" rel="me" alt="{{ social.name }}">
                <img src="data/img/about/logo/{{ social.logo }}"/>
            </a>
            {% endfor %}
        </p>
        <h3>Music</h3>
        <iframe id="music-yt-player" class="yt-player" src="" frameborder="0" allow="" hidden></iframe>
        <div id="about-music">
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Play</th>
                    </tr>
                </thead>
                <tbody>
                    {% for music in about.music %}
                    <tr>
                        <td><a href="{{ music.link }}" target="_blank">{{ music.name }}</a></td>
                        <td>
                            {% for youtube in music.youtube %}
                            <button class="youtube-show button small" data-id="{{ youtube.id }}" data-target="music">{{ youtube.name }}</button><br/>
                            {% endfor %}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        <h3>Games</h3>
        <iframe id="game-yt-player" class="yt-player" src="" frameborder="0" allow="" hidden></iframe>
        <div>
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Trailer</th>
                        <th>Links</th>
                    </tr>
                </thead>
                <tbody>
                    {% for game in about.games %}
                    <tr>
                        <td><a href="{{ game.store.link }}" target="_blank">{{ game.name }}</a></td>
                        <td>
                            {% if game.video is not null %}
                            <button class="youtube-show button small" data-id="{{ game.video }}" data-target="game"><i class="fa-solid fa-video"></i></button>
                            {% endif %}
                            {% if game.gameplay is not null %}
                            <button class="youtube-show button small" data-id="{{ game.gameplay }}" data-target="game"><i class="fa-solid fa-gamepad"></i></button>
                            {% endif %}
                        </td>
                        <td>
                            {% for link in game.links %}
                                {% if link.type == "link" or link.type == "image" %}
                                <a href="{{ link.content }}" target="_blank">{{ link.name }}</a>
                                {% endif %}
                            {% endfor %}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        <h3>Readable Stuffs</h3>
        <div>
            {% for novel in about.novels %}
            <a class="readable not-link" href="{{ novel.link }}" target="_blank" alt="{{ novel.name }}"><img src="data/img/about/readable/{{novel.image}}"/></a>
            {% endfor %}
        </div>
    </div>
    <div class="global-tab" id="faq" hidden>
        <div>
            <a class="main-link not-link" href="#faq-contact" data-category="faq-tab" data-target="faq-contact" data-dependency="faq"><h3>(+) How can I contact you?</h3></a>
            <span class="faq-tab" id="faq-contact" hidden>
                <ul>
                    <li>On Discord: Zirk#0001</li>
                    <li>By mail: contact@zirk.eu</li>
                </ul>
            </span>
        </div>
        <div>
            <a class="main-link not-link" href="#faq-dev" data-category="faq-tab" data-target="faq-dev" data-dependency="faq"><h3>(+) What is this website made with?</h3></a>
            <span class="faq-tab" id="faq-dev" data-dependency="faq" hidden>
                Made with an amazing PHP library called <a href="https://twig.symfony.com/" target="_blank">Twig</a><br/>
                I'm <a href="img/website/v1.png" target="_blank">redoing</a> <a href="img/website/v2.png" target="_blank">it</a> <a href="img/website/v3.png" target="_blank">approximately</a> <a href="img/website/v4.png" target="_blank">once</a> <a href="img/website/v5.png" target="_blank">per</a> <a href="img/website/v6.png" target="_blank">year</a>, this one isn't done yet so you can say it's V7-beta<br/>
                The source code is available on <a target="_blank" href="https://github.com/Xwilarg/zirk.eu">GitHub</a>
            </span>
        </div>
        <div>
            <a class="main-link not-link" href="#faq-cookie" data-category="faq-tab" data-target="faq-cookie" data-dependency="faq"><h3>(+) What cookies does this website use?</h3></a>
            <span class="faq-tab" id="faq-cookie" data-dependency="faq" hidden>
                This website itself doesn't use any cookies, clicking on a button in the about section will show a youtube-nocookie.com iframe which use a single "CONSENT" cookie used to store your Google preference regarding cookies<br/>
                Other than that, YouTube use your local and session storage for some things I have no idea nor control about, more information (probably) in <a href="https://policies.google.com/privacy?hl=en" target="_blank">Google privacy policy</a><br/>
                <br/>
                If you feel like this website doesn't use enough cookie, you can click on the button below that will save some random information for one hour: <button id="new-cookie" class="button small">New Cookie</button>
            </span>
        </div>
        <div>
            <a class="main-link not-link" href="#faq-rpg" data-category="faq-tab" data-target="faq-rpg" data-dependency="faq"><h3>(+) Any easter egg?</h3></a>
            <span class="faq-tab" id="faq-rpg" data-dependency="faq" hidden>
                Uh yeah sure, here you are I guess:<br/>
                <small>I'm still coding that so don't blame me if it doesn't work</small>
                <span id="rpg">
                    <div id="rpg-output">
                    </div>
                    <span id="rpg-input">
                        <input type="text" id="rpg-input-field"/>
                        <button class="button small" id="rpg-enter">Enter</button>
                    </span>
                </span>
            </span>
        </div>
        <div>
            <a class="main-link not-link" href="#faq-other" data-category="faq-tab" data-target="faq-other" data-dependency="faq"><h3>(+) Anything else?</h3>
            <span class="faq-tab" id="faq-other" data-dependency="faq" hidden>
                <img src="img/about/potato.png"/>
            </span>
        </div>
    </div>
{% endblock %}